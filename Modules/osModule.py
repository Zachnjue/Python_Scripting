# This module is used to work/interact with OS to automate many more tasks like creating directory, removing directory, identifyinh current directory etc
# This module is subdivided into 4 parts: os, os.path, os.system(), os.walk()
import os
cls = os.system("clear")
print("Your command was successful")
